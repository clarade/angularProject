import { MoviehighlightDirective } from './moviehighlight.directive';

describe('MoviehighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MoviehighlightDirective();
    expect(directive).toBeTruthy();
  });
});
